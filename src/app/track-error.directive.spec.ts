import { TrackErrorDirective } from './track-error.directive';

describe('TrackErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrackErrorDirective();
    expect(directive).toBeTruthy();
  });
});
